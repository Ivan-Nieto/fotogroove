export interface InitialValues {}

export const User = {
  isSignedIn: false,
};

export const initialValues = {
  user: User,
  userDoc: {},
  collections: [],
};

export default initialValues;
